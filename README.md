# SpiderNix

Enterprise web crawler for public data collection.

## Development

Use `nix develop` to enter the development environment.

### Commands

- `just install` - Install the package in editable mode.
- `just test` - Run the test suite.
- `just run <url>` - Run the crawler.
- `just clean` - Clean build artifacts.
